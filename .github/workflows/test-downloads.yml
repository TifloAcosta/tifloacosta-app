name: Test download feature

on:
  push:
    branches:
      - 'feature/activate-download-analyzer'
    paths:
      - 'downloads.js'
      - 'downloads-core.js'
      - 'download-config.js'
      - 'test/downloads-*.test.mjs'
      - '.github/workflows/test-downloads.yml'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm test
