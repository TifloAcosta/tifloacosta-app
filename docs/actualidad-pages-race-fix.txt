Root cause: the general GitHub Pages workflow and the Actualidad workflow could publish the same site from different snapshots. The general workflow could finish last using a commit where actualidad.json was still empty. The general Pages build now runs scripts/sync-actualidad.mjs before Jekyll builds the site. The regression test test/actualidad-pages-deploy.test.mjs enforces that ordering.
